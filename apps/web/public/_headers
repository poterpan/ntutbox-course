# Cloudflare Static Assets 的回應標頭。HSTS 與 X-Content-Type-Options 已由
# Cloudflare 端設定（實測線上已有），這裡只補其餘缺漏項，避免兩處管同一件事。
#
# ⚠️ CSP 的來源清單是**實測**出來的，不是憑印象：用 Playwright 記錄整站發出的請求，
#    且必須**點下「同意」讓 GA 真的載入**後再盤點一次——GA 同意後會另外打
#    analytics.google.com / stats.g.doubleclick.net / www.google.com.tw（地區化 pixel），
#    只在未同意狀態盤點會漏掉這三個，上線後才會被 CSP 擋掉。
#    改動前請重跑一次雙態盤點（同意前 + 同意後）。
#
# 現有來源：
#   自站 / cdn.ntutbox.com（課程資料 fetch）/ static.cloudflareinsights.com（RUM）
#   GA（僅同意後）：googletagmanager / analytics.google.com / *.google-analytics.com
#                   / stats.g.doubleclick.net / www.google.com.tw
/*
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=()
  Cross-Origin-Opener-Policy: same-origin
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https://www.google.com.tw https://www.google.com https://stats.g.doubleclick.net; font-src 'self'; connect-src 'self' https://cdn.ntutbox.com https://static.cloudflareinsights.com https://analytics.google.com https://www.google-analytics.com https://region1.google-analytics.com https://stats.g.doubleclick.net https://www.googletagmanager.com; manifest-src 'self'; worker-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'
