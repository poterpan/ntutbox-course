# 微學程功能（瀏覽＋分類＋規則原文）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 排課器新增「微學程」面板 tab：46 學程瀏覽 → 基礎/核心/總整分類課程清單＋規則原文 → 本學期開課直接排入；課程詳情反查所屬學程。

**Architecture:** 爬蟲端把每學程的 Cprog 課程標準頁（課程分類＋「相關規定」原文）併入既有 `mprograms.json`（單一 artifact）；web 端 lazy fetch 該檔、以 offering_id 反查索引接進既有 CourseLibrary tab 機制與 CourseDetailDrawer。資料走 data branch/CI，**不進本 PR**。

**Tech Stack:** Python 3.12＋Pydantic v2＋BeautifulSoup(html5lib)＋pytest；Next.js＋TS＋zustand＋vitest＋Testing Library；GitHub Actions；pnpm。

**Spec:** `docs/superpowers/specs/2026-07-19-mprogram-design.md`（本計畫的驗收基準）。

## Global Constraints

- 工作目錄＝worktree `/Users/poterpan/Documents/Coding/NTUT/ntutbox-course-mprogram`，branch `feat/mprogram`；**絕不碰 main**。以下相對路徑皆以此為根。
- 解析防呆（repo 鐵則）：html5lib；**勿寫死欄位/表格位置索引**；`<a>`/欄位缺失 → null，**勿回退預設值**；notes/規則保留原文。
- 個資紅線：任何學號/cunum/帳密不得入 repo。學校公開的聯絡教師資訊屬機構資訊，`rules_text` 照原文保存。
- Web 視覺：遵循 `apps/web/AGENTS.md`——glass token（`--ink/--ink-soft/--ink-faint/--accent/--accent-ink`）、共用元件（SearchInput/filterChipVariants/AccentButton）、圓角級距、16px input。禁 raw Tailwind 色當強調/次要文字。
- 測試指令：crawler＝`cd crawler && .venv/bin/pytest`（venv 見 Task 1）；web＝`cd apps/web && pnpm test`（vitest）、`pnpm typecheck`、`pnpm lint`。
- Commit 訊息風格比照 git log：`feat(crawler): …`／`feat(web): …`／`docs: …`，中文摘要。每個 task 至少一個 commit。
- 不可逆動作（push、開 PR）只在 Task 19，且 PR 開在 fork/origin 的 `feat/mprogram` → `main`。

---

### Task 1: 環境準備＋基線測試

**Files:** 無新增（建 `crawler/.venv`，已被 gitignore）。

- [ ] **Step 1: 建 crawler venv 並裝依賴**

```bash
cd /Users/poterpan/Documents/Coding/NTUT/ntutbox-course-mprogram/crawler
uv venv .venv && uv pip install -p .venv/bin/python -e '.[dev]'
```

- [ ] **Step 2: 跑基線測試**

Run: `cd crawler && .venv/bin/pytest -q`
Expected: 全綠（main 上為 79 tests）。任何紅 → 停下回報，不得帶病開工。

- [ ] **Step 3: 確認 web 基線**

```bash
cd ../apps/web && pnpm install && pnpm test && pnpm typecheck
```
Expected: 全綠。（pnpm workspace 於 repo 根安裝亦可，依 lockfile 位置調整。）

---

### Task 2: 實證 `e` 標記語意＋rules 抽查（研究步驟，產出映射決定）

不寫程式碼，先拿證據。spec §5 待驗證項①②。

**Files:**
- Create: `crawler/tests/fixtures/cprog_-4_mprogram_avf.html`（live 抓）
- 已存在: `crawler/tests/fixtures/cprog_-4_mprogram_av2.html`（AV2 面板，本 session 已放入）

**已知樣本**（來自 `data/canonical/standards/115.json`，位於主 worktree `../ntutbox-course/data/`）：
`notes="核e"` → division `AVF`、`AVZ`；`notes="e基"` → `AV4`、`AV7`。

- [ ] **Step 1: 抓 2 個含 e 標記學程的 live Cprog 頁**

```bash
cd /Users/poterpan/Documents/Coding/NTUT/ntutbox-course-mprogram
curl -s 'https://aps.ntut.edu.tw/course/tw/Cprog.jsp?format=-4&year=115&matric=H&division=AVF' -o crawler/tests/fixtures/cprog_-4_mprogram_avf.html
curl -s 'https://aps.ntut.edu.tw/course/tw/Cprog.jsp?format=-4&year=115&matric=H&division=AV4' -o /tmp/av4.html   # 只看不入 repo
```

- [ ] **Step 2: 檢視 notes 欄原始 HTML**，確認 `核e`/`e基` 是（a）EMI 標記與分類字併存同格，還是（b）其他語意。交叉驗證：用主 worktree `data/canonical/115-1/catalog.ndjsonl`（實際檔名以 ls 為準）找該 course_code 的 offering，看 `language` 欄是否為英語授課。

- [ ] **Step 3: rules 抽查**：對 AVF、AV4 頁面確認「相關規定」單格 `<td><font>` 區塊存在且含條文（比照 AV2）。任一頁結構不同 → 記錄差異，Task 5 的定位邏輯需涵蓋。

- [ ] **Step 4: 把結論寫進本檔**（附錄 A），格式：`e 語意=<結論>；映射=<規則>；rules 抽查=<N/N 頁有區塊>`。若證據與 Task 4 預設映射（strip 字母 e/f → emi=True；首字 基/核/總/進/應 → 對應分類）矛盾，**以證據為準修改 Task 4 再繼續**。

- [ ] **Step 5: Commit** `git add crawler/tests/fixtures/ docs/superpowers/plans/ && git commit -m "test(crawler): 微學程 Cprog fixtures + e 標記實證紀錄"`

---

### Task 3: models — `MicroProgramCourse`＋`MicroProgram` 擴充＋`SCHEMA_VERSION=2`

**Files:**
- Modify: `crawler/models.py`（`SCHEMA_VERSION` 在 :27；`MicroProgram` 在 :384-389）
- Test: `crawler/tests/test_models_mprogram.py`（新檔）

**Interfaces:**
- Produces: `MicroProgramCourse(course_code: str, name_zh: str = "", credits: float|None, category: Literal["基礎","核心","總整","進階","應用"]|None, category_raw: str|None, emi: bool = False)`；`MicroProgram` 新欄 `courses: List[MicroProgramCourse]`、`rules_text: Optional[str]`。
- 註：credits 用 `Optional[float]` 比照 `StandardCourse`（spec 寫 Decimal，此處以 repo 既有 standards 慣例為準，偏差已記錄）。

- [ ] **Step 1: 寫失敗測試**

```python
# crawler/tests/test_models_mprogram.py
from models import SCHEMA_VERSION, MicroProgram, MicroProgramCourse, MicroProgramDirectory


def test_schema_version_bumped():
    assert SCHEMA_VERSION == 2


def test_microprogram_course_fields():
    c = MicroProgramCourse(course_code="1401041", name_zh="物理", credits=3.0,
                           category="基礎", category_raw="基礎", emi=False)
    assert c.category == "基礎" and c.emi is False


def test_microprogram_new_fields_default():
    p = MicroProgram(code="AV2", name="面板微學程")
    assert p.courses == [] and p.rules_text is None


def test_directory_roundtrip():
    d = MicroProgramDirectory(term_key="115-1", programs=[MicroProgram(
        code="AV2", name="面板微學程", offering_ids=["241001"],
        courses=[MicroProgramCourse(course_code="1401041", name_zh="物理",
                                    credits=3.0, category="核心", category_raw="核e", emi=True)],
        rules_text="一、…")])
    j = MicroProgramDirectory.model_validate_json(d.model_dump_json())
    assert j.programs[0].courses[0].emi is True
```

- [ ] **Step 2: 跑測試確認失敗** — `cd crawler && .venv/bin/pytest tests/test_models_mprogram.py -q`，Expected: ImportError/AssertionError。

- [ ] **Step 3: 實作**——models.py :27 `SCHEMA_VERSION = 2`；在 `MicroProgram`（:384）前加：

```python
MProgramCategory = Literal["基礎", "核心", "總整", "進階", "應用"]


class MicroProgramCourse(BaseModel):
    """微學程課程標準列（Cprog -4，matric=H）。"""
    course_code: str
    name_zh: str = ""
    credits: Optional[float] = None
    category: Optional[MProgramCategory] = None   # notes 正規化；映不出 → None
    category_raw: Optional[str] = None            # notes 原文保底
    emi: bool = False                             # notes 內 e 標記（Task 2 實證）
```

`MicroProgram` 加：

```python
    courses: List[MicroProgramCourse] = Field(default_factory=list)
    rules_text: Optional[str] = None              # 「相關規定」原文，保留換行，不解析
```

（`Literal` 需在檔頭 typing import 補上，若尚未引入。）

- [ ] **Step 4: 全量跑** `.venv/bin/pytest -q` —— 新測試過；**既有測試若有 `schema_version == 1` 斷言，同步改 2**（grep：`grep -rn "schema_version" crawler/tests/`）。
- [ ] **Step 5: Commit** `git commit -m "feat(crawler): MicroProgram 擴充 courses/rules_text，SCHEMA_VERSION→2"`

---

### Task 4: `normalize_mprogram_category`

**Files:**
- Modify: `crawler/ntut_catalog/parse_program.py`
- Test: `crawler/tests/test_parse_program.py`（追加）

**Interfaces:**
- Produces: `normalize_mprogram_category(notes: str) -> tuple[Optional[str], bool]`（回 (category, emi)；category ∈ 五值或 None）。

- [ ] **Step 1: 失敗測試**（若 Task 2 證據與此矛盾，先改此表）

```python
import pytest
from ntut_catalog.parse_program import normalize_mprogram_category


@pytest.mark.parametrize("raw,cat,emi", [
    ("基礎", "基礎", False), ("核心", "核心", False), ("總整", "總整", False),
    ("進階", "進階", False), ("應用", "應用", False),
    ("核e", "核心", True), ("e基", "基礎", True),
    ("＊", None, False), ("", None, False), ("(e)", None, True),
])
def test_normalize_mprogram_category(raw, cat, emi):
    assert normalize_mprogram_category(raw) == (cat, emi)
```

- [ ] **Step 2: 確認失敗** — `.venv/bin/pytest tests/test_parse_program.py -q -k normalize`
- [ ] **Step 3: 實作**（parse_program.py 底部）

```python
_MPROG_CATEGORY_BY_PREFIX = {"基": "基礎", "核": "核心", "總": "總整", "進": "進階", "應": "應用"}


def normalize_mprogram_category(notes: str) -> tuple[Optional[str], bool]:
    """微學程 notes 欄 → (category, emi)。無法辨識 → (None, emi)；勿猜。"""
    raw = (notes or "").strip()
    letters = re.sub(r"[^A-Za-z]", "", raw)
    emi = "e" in letters.lower()
    rest = re.sub(r"[A-Za-z()（）]", "", raw).strip()
    if not rest:
        return None, emi
    cat = _MPROG_CATEGORY_BY_PREFIX.get(rest[0])
    return cat, emi
```

- [ ] **Step 4: 過測試**；**Step 5: Commit** `feat(crawler): 微學程 notes 分類/EMI 正規化`

---

### Task 5: `parse_cprog_rules`

**Files:**
- Modify: `crawler/ntut_catalog/parse_program.py`
- Test: `crawler/tests/test_parse_program.py`（追加）

**Interfaces:**
- Produces: `parse_cprog_rules(html: str) -> Optional[str]`。

- [ ] **Step 1: 失敗測試**

```python
FIXTURES = Path(__file__).parent / "fixtures"   # 檔內既有常數，勿重複定義


def test_parse_cprog_rules_av2():
    html = (FIXTURES / "cprog_-4_mprogram_av2.html").read_text()
    text = parse_cprog_rules(html)
    assert text is not None
    assert "微學程設置定義" in text
    assert "至少修畢8學分" in text.replace(" ", "")
    assert "\n" in text                      # 保留換行
    assert "<" not in text and ">" not in text   # 無殘留 tag


def test_parse_cprog_rules_absent():
    html = (FIXTURES / "cprog_-3_115_7.html").read_text()   # 系所列表頁，無規則區塊
    assert parse_cprog_rules(html) is None
```

- [ ] **Step 2: 確認失敗**；**Step 3: 實作**

```python
def parse_cprog_rules(html: str) -> Optional[str]:
    """Cprog -4 葉頁「相關規定」→ 純文字（保留換行）。

    結構特徵定位（禁位置索引）：恰含一個 <td> 且純文字最長的 table；
    <br> → 換行；長度 <50 字視為不存在 → None。
    """
    soup = BeautifulSoup(html, "html5lib")
    best: Optional[str] = None
    for table in soup.find_all("table"):
        tds = table.find_all("td")
        if len(tds) != 1:
            continue
        td = tds[0]
        for br in td.find_all("br"):
            br.replace_with("\n")
        lines = [ln.strip() for ln in td.get_text().splitlines()]
        text = "\n".join(ln for ln in lines if ln)
        if len(text) >= 50 and (best is None or len(text) > len(best)):
            best = text
    return best
```

（嵌套表格安全：外層 wrapper 若含課程表，`find_all("td")` 遞迴計數 >1 自然排除。）

- [ ] **Step 4: 過測試＋對 AVF fixture 加跑一次目視確認**；**Step 5: Commit** `feat(crawler): 解析 Cprog 相關規定原文（結構特徵定位）`

---

### Task 6: `crawl_mprograms` 擴充

**Files:**
- Modify: `crawler/ntut_catalog/programs.py`（`crawl_mprograms` :23-34）
- Test: `crawler/tests/test_programs.py`（`FakeProgClient` :8-22 擴充）

**Interfaces:**
- Consumes: Task 3 models、Task 4/5 parsers、既有 `parse_cprog_standard(html, entry_year, matric, division) -> ProgramStandard`、`CatalogClient.cprog(format, **params)`。
- Produces: `crawl_mprograms(client, term_key)` 回傳含 `courses`/`rules_text` 的 `MicroProgramDirectory`（簽名不變）。

- [ ] **Step 1: 失敗測試** — 擴充 `FakeProgClient`：

```python
class FakeProgClient:
    ...  # 既有 mprogram_list/mprogram/cprog 對照保留
    def cprog(self, format, **params):
        if format == "-4" and params.get("matric") == "H":
            if params.get("division") == "BROKEN":
                return "<html>查無資料</html>"
            return (FIXTURES / "cprog_-4_mprogram_av2.html").read_text()
        return ...  # 既有 -2/-3/-4 standards 對照維持原樣
```

新斷言（併入/新增 `test_crawl_mprograms_enriched`）：

```python
def test_crawl_mprograms_enriched():
    d = crawl_mprograms(FakeProgClient(), "115-1")
    p = d.programs[0]
    assert len(p.courses) == 27                       # AV2 fixture 課程數
    assert {c.category for c in p.courses} == {"基礎", "核心", "總整"}
    assert p.rules_text and "微學程設置定義" in p.rules_text
    assert all(c.course_code for c in p.courses)
```

失敗路徑：把 fake 的某 code 導向 `"BROKEN"` 回應，斷言該 program `courses == []`、`rules_text is None`、整體不拋例外。

- [ ] **Step 2: 確認失敗**；**Step 3: 實作**——`crawl_mprograms` 迴圈內、`oids` 取得後加：

```python
        courses: list[MicroProgramCourse] = []
        rules_text = None
        try:
            cprog_html = client.cprog("-4", year=year, matric="H", division=code)
            std = parse_cprog_standard(cprog_html, entry_year=year, matric="H", division=code)
            for sc in std.courses:
                if not sc.course_code:
                    continue
                category, emi = normalize_mprogram_category(sc.notes)
                courses.append(MicroProgramCourse(
                    course_code=sc.course_code, name_zh=sc.name_zh, credits=sc.credits,
                    category=category, category_raw=(sc.notes or None), emi=emi))
            rules_text = parse_cprog_rules(cprog_html)
        except Exception:
            logger.warning("[%s] cprog -4 failed for %s", term_key, code, exc_info=True)
        if rules_text is None:
            logger.warning("[%s] no rules_text for %s", term_key, code)
        programs.append(MicroProgram(code=code, name=name, offering_ids=oids,
                                     courses=courses, rules_text=rules_text))
```

（import 補 `MicroProgramCourse`、`parse_cprog_rules`、`normalize_mprogram_category`；原本 `programs.append(MicroProgram(code=..., name=..., offering_ids=oids))` 那行改為上面版本。）

- [ ] **Step 4: 全量 `.venv/bin/pytest -q` 過**；**Step 5: Commit** `feat(crawler): crawl_mprograms 併入課程標準與規則原文`

---

### Task 7: 實爬 115-1 全量驗證＋本地資料

**Files:** 產出到 worktree `data/`（gitignored，先以 `git check-ignore data` 驗證；**不 commit**）。

- [ ] **Step 1: 同步 canonical**（build_v1 需要完整 term 資料）

```bash
cd /Users/poterpan/Documents/Coding/NTUT/ntutbox-course-mprogram
rsync -a ../ntutbox-course/data/canonical/ data/canonical/
```

- [ ] **Step 2: 實爬** `cd crawler && .venv/bin/python -m ntut_catalog crawl-mprograms --terms 115-1 --out ../data`（約 46×2 請求，含既有節流，數分鐘）。
- [ ] **Step 3: 驗證統計**（python one-liner）：46 programs；`rules_text` 非 None 的 ≥ 44（少於此 → 檢視 log 找原因，不得沉默放行）；所有 courses 的 `category_raw` 值域 ⊆ Task 2 已知變體（出現新值 → 回 Task 4 補映射與測試）；AVS 的 rules_text 含「主軸」字樣（二維規則原文保留的證據）。
- [ ] **Step 4: 確認 v1 ** `ls data/v1/terms/115-1/mprograms.json` 存在且 `python -c "import json;d=json.load(open('data/v1/terms/115-1/mprograms.json'));assert d['schema_version']==2"`。
- [ ] **Step 5: 順手記錄 CDN 現況**（不阻塞）：`curl -sI https://cdn.ntutbox.com/course/v1/terms/115-1/mprograms.json | head -3` → 結果寫進附錄 A（決定 PR 描述中 stale-data 說明怎麼寫）。

---

### Task 8: `.github/workflows/crawl.yml` — 新步驟＋commit glob

**Files:** Modify: `.github/workflows/crawl.yml`

- [ ] **Step 1:** 在「Crawl」步驟之後、「Redline scan」之前插入（縮排/env 寫法比照相鄰步驟）：

```yaml
      - name: Crawl micro-programs
        run: |
          python -m ntut_catalog crawl-mprograms --terms "$TERMS" --out data
```

- [ ] **Step 2:** Commit canonical 步驟的第一個 `git add` 追加 pattern `'*/mprograms.json'`（**關鍵地雷**：漏了這行資料永遠進不了 data branch）。
- [ ] **Step 3:** `actionlint`（若未安裝：`brew install actionlint`）或至少 `python -c "import yaml;yaml.safe_load(open('.github/workflows/crawl.yml'))"` 驗語法。
- [ ] **Step 4: Commit** `ci: 每日 cron 併爬微學程並納入 data branch commit`

---

### Task 9: schema 重生成＋web types facade＋斷言同步

**Files:**
- Regenerate: `packages/schema/schema.json`、`packages/schema/index.d.ts`
- Modify: `apps/web/src/lib/data/types.ts`（facade re-export）
- Modify: web 測試中 `schema_version` 斷言（grep `toBe(1)`）

- [ ] **Step 1:** `cd packages/schema && PATH="../../crawler/.venv/bin:$PATH" pnpm generate`（`generate.py` 需 crawler 依賴；scripts=`schema`+`json-schema-to-typescript`）。
- [ ] **Step 2:** 驗證 `index.d.ts` 的 `MicroProgram` 有 `courses`/`rules_text`、新型別 `MicroProgramCourse` 存在。
- [ ] **Step 3:** `apps/web/src/lib/data/types.ts` re-export 追加 `MicroProgram, MicroProgramCourse, MicroProgramDirectory`（比照既有 export type 列表寫法）。
- [ ] **Step 4:** `grep -rn "schema_version" apps/web/src` → 把 test 斷言 `toBe(1)` 改 `toBe(2)`（若 fixture JSON 內含 `"schema_version": 1` 一併同步）。
- [ ] **Step 5:** `cd apps/web && pnpm test && pnpm typecheck` 全綠；**Commit** `feat(schema): 重生成含微學程 v2 型別`

---

### Task 10: web — `useMprograms` hook＋`buildProgramIndex`

**Files:**
- Create: `apps/web/src/lib/planner/use-mprograms.ts`、`apps/web/src/lib/planner/mprogram-index.ts`、`apps/web/src/lib/planner/mprogram-links.ts`
- Test: `apps/web/src/lib/planner/mprogram-index.test.ts`、`apps/web/src/lib/planner/use-mprograms.test.ts`

**Interfaces:**
- Produces: `useMprograms(termKey: string | null): { data: MicroProgramDirectory | null; loading: boolean; error: boolean; retry(): void }`；`buildProgramIndex(dir: MicroProgramDirectory | null): Map<string, MicroProgram[]>`；`OAA_MPROGRAM_URL`。
- Consumes: `dataBaseUrl()`（`@/lib/env`）。

- [ ] **Step 1: 失敗測試**

```ts
// mprogram-index.test.ts
import { buildProgramIndex } from "./mprogram-index";

const dir = { schema_version: 2, term_key: "115-1", programs: [
  { code: "AV2", name: "面板微學程", offering_ids: ["1", "2"], courses: [], rules_text: null },
  { code: "AV3", name: "創業家精神微學程", offering_ids: ["2"], courses: [], rules_text: null },
] } as never;

it("maps offering→programs", () => {
  const idx = buildProgramIndex(dir);
  expect(idx.get("2")!.map((p) => p.code)).toEqual(["AV2", "AV3"]);
  expect(idx.get("9")).toBeUndefined();
});
it("null-safe", () => expect(buildProgramIndex(null).size).toBe(0));
```

```ts
// use-mprograms.test.ts
import { renderHook, waitFor } from "@testing-library/react";
import { useMprograms } from "./use-mprograms";

it("fetches once and caches per term", async () => {
  const payload = { schema_version: 2, term_key: "115-1", programs: [] };
  const spy = vi.spyOn(globalThis, "fetch").mockResolvedValue(
    new Response(JSON.stringify(payload)) as never);
  const { result } = renderHook(() => useMprograms("115-1"));
  await waitFor(() => expect(result.current.data).not.toBeNull());
  renderHook(() => useMprograms("115-1"));
  expect(spy).toHaveBeenCalledTimes(1);
});

it("error state + retry", async () => {
  vi.spyOn(globalThis, "fetch").mockResolvedValue(new Response("nf", { status: 404 }) as never);
  const { result } = renderHook(() => useMprograms("999-9"));
  await waitFor(() => expect(result.current.error).toBe(true));
});
```

- [ ] **Step 2: 確認失敗**（`pnpm test -- mprogram`）；**Step 3: 實作**

```ts
// mprogram-links.ts
export const OAA_MPROGRAM_URL = "https://oaa.ntut.edu.tw/p/412-1008-13057.php?Lang=zh-tw";
```

```ts
// mprogram-index.ts
import type { MicroProgram, MicroProgramDirectory } from "@/lib/data/types";

export function buildProgramIndex(dir: MicroProgramDirectory | null): Map<string, MicroProgram[]> {
  const m = new Map<string, MicroProgram[]>();
  for (const p of dir?.programs ?? [])
    for (const oid of p.offering_ids ?? []) {
      const arr = m.get(oid);
      if (arr) arr.push(p);
      else m.set(oid, [p]);
    }
  return m;
}
```

```ts
// use-mprograms.ts
"use client";
import { useCallback, useEffect, useState } from "react";
import { dataBaseUrl } from "@/lib/env";
import type { MicroProgramDirectory } from "@/lib/data/types";

const cache = new Map<string, MicroProgramDirectory>();

export function useMprograms(termKey: string | null) {
  const [data, setData] = useState<MicroProgramDirectory | null>(
    termKey ? cache.get(termKey) ?? null : null);
  const [error, setError] = useState(false);
  const [tick, setTick] = useState(0);

  useEffect(() => {
    if (!termKey) return;
    const hit = cache.get(termKey);
    if (hit) { setData(hit); setError(false); return; }
    let alive = true;
    setData(null); setError(false);
    fetch(`${dataBaseUrl()}/terms/${termKey}/mprograms.json`)
      .then((r) => { if (!r.ok) throw new Error(String(r.status)); return r.json(); })
      .then((j: MicroProgramDirectory) => { cache.set(termKey, j); if (alive) setData(j); })
      .catch(() => { if (alive) setError(true); });
    return () => { alive = false; };
  }, [termKey, tick]);

  const retry = useCallback(() => setTick((t) => t + 1), []);
  return { data, error, loading: !!termKey && !data && !error, retry };
}
```

- [ ] **Step 4: 過測試**；**Step 5: Commit** `feat(web): 微學程資料 hook 與 offering 反查索引`

---

### Task 11: web — ui-store 擴充

**Files:**
- Modify: `apps/web/src/store/ui-store.ts`（`libraryTab` :19；比照既有 setter 寫法 :43-69）
- Test: `apps/web/src/store/ui-store.test.ts`（無則新建；有則追加）

**Interfaces:**
- Produces: `libraryTab: "courses" | "favorites" | "programs"`；`selectedProgramCode: string | null`；`setSelectedProgramCode(code: string | null)`；`openProgram(code: string)`＝一次完成 `{ libraryTab:"programs", selectedProgramCode:code, libraryOpen:true, detailOfferingId:null }`。

- [ ] **Step 1: 失敗測試**

```ts
import { useUiStore } from "./ui-store";

it("openProgram switches tab, opens panel, closes detail", () => {
  useUiStore.setState({ libraryTab: "courses", detailOfferingId: "x", libraryOpen: false });
  useUiStore.getState().openProgram("AV2");
  const s = useUiStore.getState();
  expect(s.libraryTab).toBe("programs");
  expect(s.selectedProgramCode).toBe("AV2");
  expect(s.libraryOpen).toBe(true);
  expect(s.detailOfferingId).toBeNull();
});
```

- [ ] **Step 2-4: 紅→實作→綠**（state 初值 `selectedProgramCode: null`）；**Step 5: Commit** `feat(web): ui-store 微學程 tab 與 openProgram`

---

### Task 12: web — `MicroProgramList`＋`MicroProgramPane`

**Files:**
- Create: `apps/web/src/components/planner/MicroProgramPane.tsx`、`MicroProgramList.tsx`
- Test: `apps/web/src/components/planner/MicroProgramList.test.tsx`

**Interfaces:**
- Consumes: `useMprograms`、ui-store（`selectedProgramCode`/`setSelectedProgramCode`）、term key（比照 `TermSwitcher.tsx:8-13`：`useTermStore(s => s.termKey)` 優先、fallback `useUiStore(s => s.selectedTerm)`）、`SearchInput variant="inset"`、`OAA_MPROGRAM_URL`。
- Produces: `<MicroProgramPane />`（`selectedProgramCode ? <MicroProgramDetail/> : <MicroProgramList/>`）。

- [ ] **Step 1: 失敗測試**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { MicroProgramList } from "./MicroProgramList";
import { useUiStore } from "@/store/ui-store";
import { useMprograms } from "@/lib/planner/use-mprograms";
import { OAA_MPROGRAM_URL } from "@/lib/planner/mprogram-links";

vi.mock("@/lib/planner/use-mprograms", () => ({ useMprograms: vi.fn() }));
const mocked = vi.mocked(useMprograms);
const dir = { schema_version: 2, term_key: "115-1", programs: [
  { code: "AV2", name: "面板微學程", offering_ids: ["1", "2"], courses: [], rules_text: null },
  { code: "AV3", name: "創業家精神微學程", offering_ids: ["3"], courses: [], rules_text: null },
] } as never;

beforeEach(() => useUiStore.setState({ selectedProgramCode: null }));

it("列出學程、點列選入", () => {
  mocked.mockReturnValue({ data: dir, loading: false, error: false, retry: vi.fn() });
  render(<MicroProgramList />);
  expect(screen.getByText("2 門開課")).toBeInTheDocument();
  fireEvent.click(screen.getByText("面板微學程"));
  expect(useUiStore.getState().selectedProgramCode).toBe("AV2");
  expect(screen.getByRole("link", { name: /教務處/ })).toHaveAttribute("href", OAA_MPROGRAM_URL);
});

it("過濾", () => {
  mocked.mockReturnValue({ data: dir, loading: false, error: false, retry: vi.fn() });
  render(<MicroProgramList />);
  fireEvent.change(screen.getByRole("searchbox"), { target: { value: "創業" } });
  expect(screen.queryByText("面板微學程")).toBeNull();
});

it("error 態可重試", () => {
  const retry = vi.fn();
  mocked.mockReturnValue({ data: null, loading: false, error: true, retry });
  render(<MicroProgramList />);
  fireEvent.click(screen.getByRole("button", { name: /重試/ }));
  expect(retry).toHaveBeenCalled();
});
```

（`searchbox` role 以 SearchInput 實際 render 結果為準，必要時改 `getByPlaceholderText`。）

- [ ] **Step 2-3: 紅→實作**。List 每列＝`<button>`（全寬、`rounded-xl`、hover glass）：左＝學程名（`--ink`），右＝`{offering_ids.length} 門開課`（`text-[11px] tabular-nums text-[var(--ink-soft)]`）。過濾＝`useState` query 對 `name`/`code` 子字串。空結果與 CourseLibrary 同款文案模式。底部：`<a href={OAA_MPROGRAM_URL} target="_blank" rel="noopener noreferrer">` 「教務處微學程專區（完整規定與 PDF）↗」`text-[12px] text-[var(--ink-soft)] underline`。
- [ ] **Step 4: 綠**；**Step 5: Commit** `feat(web): 微學程列表與 pane 骨架`

---

### Task 13: web — `MicroProgramDetail`

**Files:**
- Create: `apps/web/src/components/planner/MicroProgramDetail.tsx`
- Test: `apps/web/src/components/planner/MicroProgramDetail.test.tsx`

**Interfaces:**
- Consumes: props `{ program: MicroProgram }`（由 Pane 以 `selectedProgramCode` 從 `useMprograms().data` 查出後傳入）；`useTermCourses().byId`；ui-store `openDetail`、`setSelectedProgramCode(null)`（返回）。

**行為規格：**
1. Header：返回鈕（`aria-label="返回微學程列表"`）＋學程名。
2. Context copy（`text-[12px] text-[var(--ink-soft)]`）：「112 學年度起入學之日間部大學部，畢業前須完成跨領域學習（微學程為五種路徑之一）；修讀須於教務處公告期間登記。」
3. 分類分組：順序 `["基礎","核心","總整","進階","應用"]`＋`category=null` 歸「其他」（僅在非空時顯示該組）。每課列：課名＋學分（`{credits} 學分`）＋`emi && <span>英</span>`（`rounded-md` badge、`--ink-soft` 邊框字）。
4. 課的本學期班級 chips：建 `offeringsByCode = useMemo(...)`——遍歷 `program.offering_ids`，`byId(oid)` 取 `CourseOffering`，依 `course_code` 分桶；`byId(oid)` 為 undefined（offering 不在 catalog，理論不發生）→ 該 oid 以純文字列出（spec §4 防呆）。有班 → 每班一個 chip（`filterChipVariants({active:false})`，文字＝班級名或 offering_id，`onClick={() => openDetail(oid)}`）；無班 → `<span className="text-[11px] text-[var(--ink-faint)]">本學期未開</span>`。
5. `courses.length === 0`（舊資料/解析失敗）→ 顯示「課程標準暫缺，僅列本學期開課」＋把 `offering_ids` 直接以 chips 列出（同樣 openDetail）。
6. 規則原文：`rules_text` → `<details>`（summary=「修讀規則（原文）」）內 `<p className="whitespace-pre-line text-[13px] leading-relaxed">`；`null` → 「規定原文暫缺，請以教務處專區為準」。兩種情況下方都放 OAA 外連。

- [ ] **Step 1: 失敗測試**：mock program（2 分類、1 課有班、1 課無班、rules_text 含換行）＋term-store 種子課（比照 `CourseListItem.test.tsx` 的 `beforeEach` setState 模式）。斷言：分組標題順序；「本學期未開」出現；點班級 chip → `detailOfferingId` 被設；`rules_text` 段落存在；`rules_text:null` 變體顯示暫缺文案；返回鈕清 `selectedProgramCode`。
- [ ] **Step 2-4: 紅→實作→綠**；**Step 5: Commit** `feat(web): 微學程詳情——分類清單/班級 chips/規則原文`

---

### Task 14: web — RightPanel 第三個 tab 接線

**Files:**
- Modify: `apps/web/src/components/planner/PlannerLayout.tsx`（RightPanel :156-174）
- Test: `apps/web/src/components/planner/PlannerLayout.test.tsx`（追加案例）

- [ ] **Step 1: 失敗測試**：render 後點「微學程」tab → MicroProgramList 的可辨識元素出現（mock useMprograms）。
- [ ] **Step 2-3:** `<PanelTab active={tab === "programs"} onClick={() => setTab("programs")}>微學程</PanelTab>`；內容區改三態：`{tab === "courses" ? <CourseLibrary /> : tab === "favorites" ? <FavoritesList 包裝維持原樣 /> : <MicroProgramPane />}`（favorites 的既有 scroll wrapper 照舊）。
- [ ] **Step 4: 綠＋既有 PlannerLayout 測試不破**；**Step 5: Commit** `feat(web): 排課面板新增微學程 tab`

---

### Task 15: web — CourseDetailContent 學程 chips

**Files:**
- Modify: `apps/web/src/components/planner/CourseDetailContent.tsx`（插在 Dcard chip 區塊 :133 之後、`<dl>` :135 之前；樣式仿 `DcardChip` :231-245）
- Test: `apps/web/src/components/planner/CourseDetailContent.test.tsx`（有則追加，無則建最小案例）

- [ ] **Step 1: 失敗測試**：mock `useMprograms` 回含該 offering 的學程 → 「面板微學程」chip 出現；點擊 → `libraryTab==="programs"` 且 `selectedProgramCode==="AV2"` 且 `detailOfferingId===null`。無所屬學程 → 區塊不渲染。
- [ ] **Step 2-3: 實作**：

```tsx
const { data: mprogDir } = useMprograms(termKey);
const programChips = useMemo(
  () => buildProgramIndex(mprogDir).get(c.offering_id) ?? [], [mprogDir, c.offering_id]);
...
{programChips.length > 0 && (
  <div className="flex flex-wrap gap-1.5">
    {programChips.map((p) => (
      <button key={p.code} type="button" onClick={() => openProgram(p.code)}
        className="inline-flex items-center gap-1 rounded-full bg-[var(--accent)]/10 px-2.5 py-1 text-[12px] font-medium text-[var(--accent-ink)]">
        {p.name}
      </button>
    ))}
  </div>
)}
```

（`termKey` 取法與 Task 12 相同；`openProgram` 已含關 drawer。className 以 DcardChip 實際樣式為準對齊。）

- [ ] **Step 4: 綠**；**Step 5: Commit** `feat(web): 課程詳情顯示所屬微學程並可跳轉`

---

### Task 16: docs 修正（同 PR）

**Files:**
- Modify: `docs/DESIGN.md`（mprograms artifact 說明處，:57 端點表附近的 artifacts 章節）
- Modify: `CLAUDE.md`（「現況」段）
- Modify: `docs/superpowers/specs/2026-07-19-mprogram-design.md`（兩處事實修正）

- [ ] **Step 1: DESIGN.md** 在 mprograms.json 相關段落補 v2 schema 摘要：

```markdown
- `mprograms.json` v2（SCHEMA_VERSION=2）：每學程除 `offering_ids` 外新增
  `courses[]`（course_code/name_zh/credits/category(基礎|核心|總整|進階|應用|null)/category_raw/emi，
  來源 Cprog -4 matric=H，notes 欄正規化）與 `rules_text`（「相關規定」原文，保留換行不解析）。
```

- [ ] **Step 2: CLAUDE.md 現況段**——「未爬（P1 需要再做）：課程描述/課綱詳情檔、微學程、課程標準（requirement.category 對映）」整行改為：

```markdown
- 課程描述/課綱詳情（details.ndjson）、微學程（mprograms.json v2：開課+分類課程+規則原文）、
  課程標準（standards/<entry_year>.json）皆已爬；微學程 Web UI 見 feat/mprogram PR。
```

- [ ] **Step 3: spec 修正**：§2「沿用既有 SW 快取策略」→「快取＝hook 內記憶體 cache（web 目前尚無 Service Worker；PWA 為既有 roadmap 項）」；§6「重爬的 data/ 產物」→「data 產物走 data branch/CI（main PR 不含資料）；merge 後以 workflow_dispatch 觸發 crawl.yml 重爬並發佈 R2」。
- [ ] **Step 4: Commit** `docs: 微學程 v2 schema/現況修正/spec 事實勘誤`

---

### Task 17: 本地視覺驗證

**Files:** 無 repo 變更（`apps/web/public/data` 若非 gitignore，**不得 git add**，先 `git check-ignore apps/web/public/data || echo NOT-IGNORED` 確認）。

- [ ] **Step 1:** `mkdir -p apps/web/public/data && cp -R data/v1 apps/web/public/data/`（dev 預設 base=`/data/v1`，見 `apps/web/src/lib/env.ts`）。
- [ ] **Step 2:** `cd apps/web && pnpm dev`，用 chrome-devtools MCP 開 `http://localhost:3000`。
- [ ] **Step 3: 檢查清單**（桌面 1440px light/dark ＋ 行動 390px，各截圖存 scratchpad）：
  - 微學程 tab 出現、46 筆、過濾可用；點入 AV2：三分組、班級 chips 可點開 drawer、「本學期未開」灰態存在（找一個有未開課的學程，如 AZF 檢查跨校課不出現、本校 4 門正確）。
  - 規則原文換行正確（「一、二、…」逐行）、`<details>` 摺疊可開合；AVS 詳情含「主軸」規則原文。
  - 課程詳情 drawer：屬於某學程的課顯示 chip；點 chip → drawer 關、panel 切到該學程。
  - 對比度／honest gaps（使用者既定偏好）：灰態與暫缺文案清楚可讀，dark mode 不糊。
- [ ] **Step 4:** 發現視覺/行為問題 → 修正 → 補跑對應測試 → commit（訊息註明修了什麼）。

---

### Task 18: 全量驗證＋獨立驗收＋review

- [ ] **Step 1: 全量**：`cd crawler && .venv/bin/pytest -q`；`cd apps/web && pnpm test && pnpm typecheck && pnpm lint && pnpm build`（build script 名以 package.json 為準）。全綠才往下。
- [ ] **Step 2: fresh-context 驗收 agent**（不給實作結論，只給條件）逐條核實：spec §1-§4 每項行為、Task 7 統計、crawl.yml 兩處變更存在、docs 三處修正存在、測試綠證據。任何 fail → 修 → **重新驗收**。
- [ ] **Step 3: code review 雙軌**：`feature-dev:code-reviewer`（審 `main...feat/mprogram`，重點：解析防呆鐵則、schema 相容、store 競態、XSS——`rules_text` 必須以純文字渲染不得 dangerouslySetInnerHTML）＋ codex（`--model gpt-5.5`）二輪意見。**codex 的每個 finding 須逐一對照程式碼驗證後才採用**（使用者指示：不一定正確，自行斟酌），採納/駁回各留一行紀錄。
- [ ] **Step 4:** 修正採納的 findings → 補測試 → 全量重跑綠 → commit。

---

### Task 19: push＋PR

- [ ] **Step 1:** `git push -u origin feat/mprogram`
- [ ] **Step 2:** `gh pr create --title "feat: 微學程瀏覽——分類課程清單/規則原文/排入課表（web+crawler）" --body <<EOF`，body 含：摘要（三層：crawler v2 / web tab / CI+docs）、截圖、測試證據、**Post-merge checklist**：① workflow_dispatch 觸發 crawl.yml（含新 mprograms 步驟）② 確認 data branch 有 mprograms.json v2、R2 已發佈 ③ prod 視覺抽查。並註明：merge 後、資料重爬前，prod 讀到的是 v1 舊檔（無 courses/rules_text），UI 以「課程標準暫缺」降級呈現（Task 13 行為 5）。
- [ ] **Step 3:** 通知使用者：PR 連結＋一段人話總結＋post-merge 待辦。

---

## 附錄 A：執行期實證紀錄（Task 2/7 填寫）

- e 語意：（待填）
- 映射修正：（待填/無）
- rules 抽查：（待填 N/N）
- CDN mprograms 現況：（待填）
